变价流程

1. 变价流程逻辑调整为先调用 dpe 再调用 ComData。1pd

2. 新鲜度过期时增加兜底佣金率，兜底补贴（方案待确认）。1pd

   

采集流程

流程部分

1. 采集数据可用性与定价配置追价层级映射匹配与过滤。 1.5pd
2. 支持数据质量不匹配时自动降级重新匹配。1pd
3. 底价模式卖价分层价、商促折扣、会员折扣等字段整理给小刚和映射装换。2pd
4. 定价服务取数节点改动。1pd
5. 佣金率落库，数据库变更部分。1pd
6. 灰度控制部分逻辑开发。 2pd

计算部分

1. MT原价-COM原价 计算逻辑。1pd
2. MT商促价-COM商促价 计算逻辑。1.5pd
3. MT供应链价格-COM供应链价格 计算逻辑。2pd
4. 支持根据数据质量自动降级和根据配置项去最大值逻辑。 2pd
5. dpe 佣金率交互逻辑开发。1 pd
6. 全局兜底佣金率逻辑。1pd

自测

1. 线上流量录制与回放。1pd
2. 流程改动点，对其他业务场景的影响check和回归测试。1pd
3. 计算部分逻辑自测 3pd。

计算部分

联调

1. 和 mtData 联调 1pd
2. 和 dep 连天 1pd

